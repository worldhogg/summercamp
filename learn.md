# Дневник обучения

## День 1
- Установил среду: IDE, компилятор, git
- Настроил SSH-ключ
- Первый commit и push

## День 2
- Прошёл learngitbranching.js.org (основы + ветки + remotes)
- Решил Two Sum на LeetCode (1/75 Blind)
- Понял паттерн "хеш-таблица для поиска"
## День 3
- Глубоко изучил теорию хеш-таблиц: хеш-функции, коллизии, метод цепочек, рехеширование, load factor
- Прочитал несколько статей (Habr и др.), посмотрел видео The Cherno
- Разобрался с разницей между std::map / std::unordered_map / std::set / std::unordered_set
- Понял когда что использовать: сортированный порядок vs скорость, O(log n) vs O(1)
- Попрактиковался с API std::unordered_map в IDE
- Готов к практике задачами
## День 4
- Решил Valid Parentheses через std::stack (3/75 Blind)
- Решил Contains Duplicate через std::unordered_set (4/75 Blind)
- Решил Valid Anagram через std::unordered_map (5/75 Blind)
- Решил первую Medium: Group Anagrams (6/75 Blind)
- Освоил базовый CMake — собрал тестовый проект через build-систему
## День 5
- Решил Top K Frequent Elements (6/75 Blind), разобрал 3 подхода к задаче:
  - В лоб через unordered_map + поиск максимума: O(n*k)
  - Через priority_queue (min-heap): O(n log k) — отложил до изучения теории кучи
  - Через bucket sort: O(n) — оптимально
- Получил подробное код-ревью на первое решение: важность правильного именования переменных, понимание сложности, привычка к code review
- Решил Happy Number (7/75 Blind) — паттерн "детекция цикла через unordered_set"
- Решил Isomorphic Strings (8/75 Blind) — двусторонний маппинг через два unordered_map. Получил разбор бага: важно обрабатывать оба случая (символ есть в мапе / символа нет в мапе), мысленно прогонять код на примерах перед Submit
- Решил Intersection of Two Arrays II (9/75 Blind) — подсчёт частот через unordered_map
- Longest Consecutive Sequence — не зашло сегодня, перенёс на день 6 со свежей головой
## День 6
- Перерешал Two Sum локально без подсказок — закрепил хеши на автомате
- Решил Longest Consecutive Sequence через unordered_set с трюком "стартуем без предшественника" (10/75 Blind). Сам дошёл до 80% идеи, понял оптимизацию O(n²) → O(n)
- Изучил теорию префиксных сумм: что это, как через них считать сумму подмассива за O(1)
- Решил Subarray Sum Equals K в лоб через два цикла (11/75 Blind)
- Паттерн "префикс-суммы + хеш-таблица" оказался сложным с первого захода — отложил на повторение через 2-3 недели

### Итог недели 1
- 11 задач Blind 75 решено
- Освоены: git, базовый CMake, std::vector, std::stack, std::unordered_map, std::unordered_set
- Понятие сложности алгоритмов: O(1), O(log n), O(n), O(n log n), O(n²)
- Привычка ежедневных коммитов и дневника
- Code review на свой код — учусь видеть свои ошибки
- Готов к неделе 2: модерн C++ (RAII, smart pointers, move-семантика, лямбды)
## День 8 — старт недели 2

### Темы дня
- RAII — главная идиома C++, привязка времени жизни ресурса к времени жизни объекта
- Smart pointers как пример RAII для динамической памяти
- std::unique_ptr — единственный владелец, нельзя копировать, можно перемещать
- std::shared_ptr — общий владелец со счётчиком ссылок
- std::weak_ptr — упомянуто, углублю позже

### Практика
- Поэкспериментировал с unique_ptr и shared_ptr в `smart_pointers_playground.cpp`
- Увидел, как работают конструкторы/деструкторы при выходе из области видимости
- Понял, почему unique_ptr нельзя копировать (понял через ошибку компилятора)
- Увидел счётчик shared_ptr в работе

### Разминка
- Перерешал Contains Duplicate за 5 минут — хеши на автомате

### Итог
- Готов завтра закрепить smart pointers практической задачей
## День 9

### Темы дня
- 3 сценария передачи unique_ptr в функцию: по ссылке, через move, как возвращаемое значение
- Метод .get() и почему он не передаёт владение
- unique_ptr<T[]> для динамических массивов
- Правило трёх и правило пяти — зачем нужны 5 специальных методов
- Подготовился к написанию MyString завтра

### Практика
- Решил Word Pattern на LeetCode (12/75 Blind) — паттерн двух маппингов из Isomorphic Strings
- Создал структуру проекта MyString: CMakeLists.txt, заголовочные файлы, README

### Понимание
- Понял, почему std::unique_ptr нельзя копировать (нарушение единственного владения)
- Увидел, какие 3 типа функций бывают (использующие / получающие владение / создающие объект)
- Готов завтра шаг за шагом написать класс MyString с правилом пяти
## День 10

### Темы дня
- Написал свой класс MyString — первый серьёзный класс лета
- Освоил правило трёх (Rule of Three) на практике
- Понял разницу между поверхностным и глубоким копированием
- Изучил idiom copy-and-swap для безопасного присваивания
- Защита от самоприсваивания (`this == &other`)
- Возврат *this из оператора присваивания для цепочек

### Реализованные методы
- Конструктор по умолчанию
- Конструктор из C-строки
- Деструктор
- Конструктор копирования (глубокое копирование)
- Оператор присваивания (copy-and-swap)
- size(), c_str()
- operator[] (const и не-const версии)
- Дружественный operator<< для вывода

### Что узнал
- Почему дефолтные методы копирования опасны для классов с указателями
- Зачем нужен список инициализации (`: field_(value)`)
- Что значит const после метода
- Что такое friend-функция и зачем нужна для operator

### Прошло 7 тестов в main.cpp
Завтра: move-семантика (правило 5)
## День 11

### Темы дня
- Move-семантика: зачем нужна, когда применяется
- lvalue vs rvalue — разница и примеры
- rvalue-ссылки (`&&`) — синтаксис и смысл
- `std::move` — НЕ двигает, а помечает как rvalue
- noexcept для move-операций (требование STL)

### Реализованное в MyString
- Конструктор перемещения (Rule of Five — 4/5 ✅)
- Оператор перемещающего присваивания (Rule of Five — 5/5 ✅)
- ПРАВИЛО ПЯТИ ПОЛНОСТЬЮ РЕАЛИЗОВАНО

### Сегодня прошёл через 3 типа ошибок (важный опыт):
1. Запуск не того бинарника (старый файл из другого проекта)
2. Compiler error: "redefinition" — случайно скопировал оператор присваивания дважды
3. Linker error: "Undefined symbols" — удалил реализацию конструктора копирования при чистке
Научился различать ошибки компилятора и линковщика — это разные стадии сборки.

### Тесты
- Тест 8: move-конструктор — после move source пуст ✅
- Тест 9: move-присваивание — содержимое передано ✅
- Тест 10: возврат из функции — RVO/move работает ✅
- Все 10 тестов из main.cpp проходят

### Понимание
- Увидел разницу между копированием и перемещением через отладочные принты
- Понял "valid but unspecified state" после move
- Понял, почему компилятор не генерирует move сам при пользовательском деструкторе
- Понял разницу между ошибками компилятора и линковщика

### LeetCode
- Решил Single Number через XOR (13/75 Blind) — паттерн "магия побитовых операций"